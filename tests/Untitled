import pytest
from app import create_app  # Или: from app import app

@pytest.fixture
def app():
    """Создаёт и настраивает тестовое приложение"""
    # ВАРИАНТ 1: Если у вас factory pattern (create_app)
    application = create_app('testing')
    application.config.update({
        'TESTING': True,
        'WTF_CSRF_ENABLED': False,
    })